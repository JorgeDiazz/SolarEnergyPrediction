/// Represents the temperature for a selected location on the map
class Temperature {
  final int average;
  final int max;
  final int min;

  Temperature({
    required this.average,
    required this.max,
    required this.min,
  });
}
